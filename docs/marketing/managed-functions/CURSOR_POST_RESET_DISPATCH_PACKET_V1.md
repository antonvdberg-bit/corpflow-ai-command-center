# Cursor post-reset dispatch packet — Managed Functions v1

**Status:** READY TO DISPATCH AFTER CURSOR RESET / docs-only  
**Source:** #1274 / PR #1275  
**No runtime authorization is granted by this document.**

## Operator dispatch principle

Do not send Cursor the full strategic conversation. Send one bounded packet at a time.

Cursor is not being asked to decide positioning, design a CMS, research SEO, or invent content. Those decisions are already made in PR #1275.

## Packet A dispatch — Insights ingestion

**Class:** S

### Prompt

> Implement Packet A from `docs/marketing/managed-functions/CURSOR_IMPLEMENTATION_CLASSIFICATION_V1.md` using the source copy in PR #1275. Work only in the existing repository-managed Insights engine. Add the four flagship Managed Functions / Managed Growth Insight records to `lib/public/insights-content.js`. Reuse the existing `/insights` index, `/insights/[slug]` renderer, current public shell, current metadata helper, current hero assets, and existing sitemap integration. Do not create a CMS, DB table, API, MDX framework, new visual system, new analytics vendor, or new content abstraction. Keep Lead Rescue runtime/commercial behavior unchanged. Add only the minimum tests needed to prove the four slugs render through the existing content API and sitemap path generation. Return changed files, branch/PR, local test/build evidence, and preview status. Stop before merge or production deploy.

### Definition of done
Four new Insight pages are generated by the existing engine on a reviewable preview, without introducing new infrastructure.

### Expected files
- `lib/public/insights-content.js`
- bounded test file(s) only

Do not edit the renderer unless a proven defect blocks the content.

## Packet B dispatch — Managed Growth + Growth Leakage Review

**Class:** M

### Prompt

> Implement Packet B from `docs/marketing/managed-functions/CURSOR_IMPLEMENTATION_CLASSIFICATION_V1.md`. Add two public pages only: `/managed-growth` and `/growth-leakage-review`. Compose them from existing CorpFlowAI public primitives (`CorpFlowPublicPhotoShell`, `PublicHero`, `OutcomeSection`, `PublicCtaBand`, `PublicTrustBand`, existing styles and `buildPublicPageMeta`). Source approved copy from `MANAGED_GROWTH_LANDING_PAGE_V1.md` and `GROWTH_LEAKAGE_REVIEW_V1.md`. Reuse existing governed visual assets for preview; do not create an asset-generation subproject. Route the Growth Leakage Review CTA into the existing contact/discovery mechanism unless a separately approved intake already exists. No DB, schema, env, payment, outbound messaging, CMS, new app, tenant change, or paid tool. Add minimum route/CTA/meta tests. Produce a preview and stop for operator review before any merge/deploy.

### Definition of done
Two reviewable public pages exist on preview, clearly explain the Managed Growth function and diagnostic, and use existing infrastructure only.

## Packet C dispatch — apex narrative reconciliation

**Class:** S–M

### Prompt

> Implement Packet C using `SITE_COPY_RECONCILIATION_V1.md` and `SOURCE_PATH_AUDIT_AND_EXACT_CHANGESET_V1.md`. Reconcile only company-level copy/navigation on `components/CorpFlowPublicHome.js`, `lib/public/corpflow-public-market.js`, `pages/about.js`, `pages/services.js`, and the `/insights` index where needed. Preserve Lead Rescue as the current concrete paid wedge and keep its route, price, CTA, payment rules and conversion mechanism unchanged unless the operator separately authorizes a commercial change. Replace the stale About language `CorpFlowAI is not a growth platform` with the approved distinction between a growth-software platform and an operations company that manages defined business functions. Make the company proposition international by default while leaving current Mauritius-specific Lead Rescue terms accurate. No site-wide redesign, no tenant surfaces, no operator/admin changes. Return exact before/after copy evidence and preview screenshots. Stop before production.

### Definition of done
A first-time buyer can understand CorpFlowAI as a managed-business-function company, Managed Growth as the first family, and Lead Rescue as a specific current offer without contradictory copy.

## Packet D dispatch — SEO / AI discoverability verification

**Class:** S

### Prompt

> Implement Packet D using `SEO_AI_DISCOVERABILITY_V1.md`. Do not install paid or new SEO tooling. Add `/managed-growth` and `/growth-leakage-review` to the apex `APEX_PATHS` in `pages/sitemap.xml.js`; verify the four new Insight paths flow automatically from `listInsightsForStaticPaths()`. Verify page titles, descriptions and canonicals use existing `buildPublicPageMeta`. Confirm `public/robots.txt` continues to allow public pages and block operator/private surfaces; do not weaken private-path disallows. Add only appropriate structured data already supported by the stack, if useful; do not invent special GEO schema. Add bounded assertions for sitemap, canonicals, key internal links and public crawler accessibility. Return evidence and stop before production deploy.

### Definition of done
New pages are crawlable, canonically described, internally linked and represented in the existing sitemap with no new SEO infrastructure.

## Cursor spend controls

### Mandatory
- Run one packet at a time.
- Reuse existing components and data structures.
- Read the source docs before generation.
- Do not research strategy or rewrite approved copy unless a factual/runtime incompatibility is proven.
- Do not create additional agents for failed CI without explicit operator instruction.
- If CI fails for unrelated baseline reasons, report the exact failure before generating repair work.

### Stop immediately if
- estimated implementation becomes L-class;
- a new platform/CMS/database is proposed;
- more than ~12 runtime/test files are needed for a single packet;
- payment/env/schema/outbound messaging changes appear necessary;
- tenant routing would be affected;
- a production action is needed;
- approved copy cannot fit current components without a broad refactor.

## Recommended sequencing after reset

`A -> preview/evidence -> B -> preview/Anton visual review -> C -> preview/evidence -> D -> full preview verification -> Anton production approval`

This sequence intentionally puts low-cost editorial ingestion first and defers broader apex narrative changes until the new validation pages visibly exist.