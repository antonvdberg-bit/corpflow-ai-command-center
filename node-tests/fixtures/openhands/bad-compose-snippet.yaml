name: fixture-bad

services:
  fixture-bad-app:
    image: docker.openhands.dev/openhands/openhands:latest
    container_name: fixture-bad-app

    ports:
      - "0.0.0.0:3000:3000"

    privileged: true
    network_mode: host

    environment:
      POSTGRES_URL: "postgres://should-never-be-here"
