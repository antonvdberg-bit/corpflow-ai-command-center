import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); export default async function handler(req, res) { if (req.method !== "POST") return res.status(405).end(); const { name, email, intent } = req.body; try { const baseUrl = process.env.BASEROW_URL?.includes("http") ? process.env.BASEROW_URL : `https://api.baserow.io`; await fetch(`${baseUrl}/api/database/rows/table/${process.env.BASEROW_TABLE_ID}/?user_field_names=true`, { method: "POST", headers: { "Authorization": `Token ${process.env.BASEROW_TOKEN}`, "Content-Type": "application/json" }, body: JSON.stringify({ "Name": name, "Email": email, "Notes": intent }) }); return res.status(200).json({ status: "Sync Active" }); } catch (e) { return res.status(200).json({ status: "Captured" }); } finally { await prisma.$disconnect(); } }
