# #752 Viewing by Invitation — runtime evidence (synthetic)

Synthetic-only fixture. No real private client data. No live send.

| File | Purpose |
|------|---------|
| `synthetic-viewing-invitation-draft.txt` | Copy-ready viewing invitation draft from a qualified/shortlisted/presentation-ready lead |
| `synthetic-viewing-invitation-packet.json` | Invitation readiness checklist + `send_disabled: true` metadata |

Generated by the helper `lib/cmp/_lib/lux-lead-viewing-by-invitation.js` (same path used by Lux `/change` list/patch).

**Confirmations for PR review**

- Live send disabled (`send_disabled: true`)
- Public Lux marketing files not modified by this slice
- Storage uses existing `leads.qualification_json.private_client_viewing` (no schema migration)
