# EXAMPLE ONLY — copy to the server as ~/corpflowai-beszel/.env (mode 600).
# Never commit a filled copy. Never paste real values into chat or git.
#
# Canonical runbook: docs/operations/BESZEL_SERVER_UTILISATION_PILOT.md
#
# Image tags are pinned in compose.example.yml (0.18.7 at authorship).
# Override here only if the server compose uses ${BESZEL_*_TAG} substitution.

# Generated in the Beszel hub UI when adding the local system. Placeholders only here.
BESZEL_AGENT_TOKEN=REPLACE_ME_BESZEL_AGENT_TOKEN
BESZEL_AGENT_KEY=REPLACE_ME_BESZEL_AGENT_KEY
